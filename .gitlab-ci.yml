image: node:latest

cache:
    paths:
        - node_modules/
        
before_script:
    - npm install
    
stages:
    - build
    
build_prod:
    stage: build
    only: stable/0.1
    script:
        - npm run prod